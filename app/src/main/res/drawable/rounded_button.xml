<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna latar tombol -->
    <solid android:color="#3F51B5" />

    <!-- Sudut melengkung -->
    <corners android:radius="27dp" />

    <!-- Opsional: padding di dalam tombol -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="6dp"
        android:bottom="6dp" />
</shape>
